/**
 * Domain Layer — Entities
 *
 * Pure business types. No framework, database, or UI dependencies.
 * This is the innermost layer per Clean Architecture — everything else
 * depends on this, never the other way around.
 */

export * from "./entities/index"
